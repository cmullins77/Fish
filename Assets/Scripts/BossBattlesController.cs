using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossBattlesController : MonoBehaviour
{
    public static readonly BossBattlesController Instance = new BossBattlesController();
    private BossBattlesController()
    {

    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
